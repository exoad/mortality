import 'package:flutter/material.dart';

class MortalityAppView extends StatelessWidget {
  const MortalityAppView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Amogus")));
  }
}
